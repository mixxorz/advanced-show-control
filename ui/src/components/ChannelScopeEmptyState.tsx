import { Panel } from "./Panel";

export function ChannelScopeEmptyState() {
  return (
    <Panel className="p-4 text-sm text-console-muted">
      Store the current mixer state to choose scoped channels.
    </Panel>
  );
}
